import 'package:flutter/material.dart';

class  SalaTimesScreen extends StatelessWidget {
  const  SalaTimesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
